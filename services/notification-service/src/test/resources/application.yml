spring:
  cloud:
    consul:
      enabled: false
      discovery:
        enabled: false
  kafka:
    bootstrap-servers: localhost:9092

sendgrid:
  api-key: SG.test-placeholder

twilio:
  account-sid: ACtest
  auth-token: testtoken
  from-number: "+15005550001"

notification:
  from-email: noreply@ecommerce.local

logging:
  level:
    com.ecommerce.notificationservice: INFO
